version: 0.2

phases:
  install:
    runtime-versions:
      java: 17
    commands:
      - chmod +x ./gradlew
  build:
    commands:
      - ./gradlew build -x test

artifacts:
  files:
    - build/libs/*.jar
    - appspec.yml
    - scripts/**
    - src/main/resources/static/** # <--- 이 줄 추가!